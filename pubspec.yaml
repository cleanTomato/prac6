name: prac6
description: "A new Flutter project."

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.3.1

  # Пример подключения пакета provider из GitHub:
  # provider:
  #   git:
  #     url: https://github.com/rrousselGit/provider
  #     ref: main  # можно указать: main, develop, v1.0.0, или хэш коммита
  #
  # Пример подключения пакета из конкретной папки репозитория:
  # my_custom_widgets:
  #   git:
  #     url: https://github.com/example/flutter-widgets
  #     path: packages/custom_widgets  # путь к пакету внутри репозитория
  #     ref: v2.1.0  # конкретная версия (тег)

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true


